<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="p-3 bg-dark text-white">
      <div class="container">
        <nav class="d-flex flex-wrap align-items-center ">
          <ul class="nav col-12 col-lg-auto me-lg-auto mb-2  mb-md-0 nav justify-content-lg-start">
            <li> <%= link_to 'Home', welcome_path, class: 'nav-link px-2 text-white' %></li>
            <li> <%= link_to 'Posts', posts_path, class: 'nav-link px-2 text-white' %></li>
          </ul>
          <ul class="nav col-12 col-lg-auto mb-2 mb-md-0 nav justify-content-end">
            <% if logged_in? %>
              <li> <%= link_to 'Log out', log_out_path, class: 'nav-link px-2 text-white',
                               data: {method: :delete}%></li>
            <% else %>
              <li> <%= link_to 'Sign in', login_path, class: 'nav-link px-2 text-white' %></li>
              <li> <%= link_to 'Create account', new_user_path, class: 'nav-link px-2 text-white' %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>

    <main class="container mt-3">
      <%= yield %>
    </main>
  </body>
</html>
